import React from 'react';
import { ScrollableContainer, Text } from '../../components';

export default function Menu() {
  return (
    <ScrollableContainer>
      <Text>Menu</Text>
    </ScrollableContainer>
  );
}
